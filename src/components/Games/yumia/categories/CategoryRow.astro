---
const { item, lang, game } = Astro.props;
---

<tr>
  <td>
    <a href={`/${game}/items/${item.id}/${lang}`}>{item.name[lang]}</a>
    {item.isDLC && <span class="badge bg-primary">DLC</span>}
  </td>
</tr>
